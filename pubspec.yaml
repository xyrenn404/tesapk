name: whatsapp_pairing
description: A new Flutter project for WhatsApp pairing.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  whatsapp_unilink: ^2.1.0
  qr_flutter: ^4.1.0
  shared_preferences: ^2.2.2
  http: ^1.1.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true